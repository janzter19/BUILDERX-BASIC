# Database package

`builderx-basic-schema.sql` is a schema-only export for the reusable
BUILDERX-BASIC release. It contains no live row data and no credentials.

Import it into a newly created `rbmsv4` database before configuring the web
application. Database usernames and passwords are installation-specific and
must be created locally by the target computer's database administrator.
